# Template only -- copy to secret.yaml, fill in real values, and never
# commit the filled-in version (add k8s/secret.yaml to .gitignore first).
# Apply with: kubectl apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: proxy-secrets
  namespace: proxy
type: Opaque
stringData:
  NVIDIA_API_KEY: "replace-me"
  SUPABASE_JWT_SECRET: "replace-me"
  ADMIN_API_KEY: "replace-me"
  NEO4J_PASSWORD: "replace-me"
  QDRANT_API_KEY: "replace-me"
  TAVILY_API_KEY: "replace-me"
